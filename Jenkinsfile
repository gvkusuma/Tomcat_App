pipeline
{
    agent any
    stages{ 
        stage('Init')
        {
           echo "Starting the pipeline testing"
        }
        stage('Build')
        {
            echo "Build the project"
        }
        stage('Deploy')
        {
            echo "Deploy the project"
        }
    }
}
